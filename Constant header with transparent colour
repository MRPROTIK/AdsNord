<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        :root {
            /* Extracted from your logo */
            --google-blue: #4285F4;
            --google-green: #34A853;
            --google-yellow: #FBBC05;
        }

        body {
            margin: 0;
            padding: 0;
            background: #020617; /* Deep aurora base */
            font-family: 'Inter', -apple-system, sans-serif;
            color: white;
            height: 300vh; /* For scrolling */
            /* Aurora Mesh Background */
            background-image: 
                radial-gradient(at 0% 0%, rgba(66, 133, 244, 0.2) 0px, transparent 50%),
                radial-gradient(at 100% 0%, rgba(52, 168, 83, 0.2) 0px, transparent 50%);
            background-attachment: fixed;
        }

        /* --- THE CONSTANT GLASS HEADER --- */
        header.constant-glass-nav {
            position: fixed;
            top: 15px; /* Floating gap */
            left: 50%;
            transform: translateX(-50%);
            width: 94%;
            max-width: 1100px;
            background: rgba(255, 255, 255, 0.85); /* Strong white glass */
            backdrop-filter: blur(25px) saturate(180%);
            -webkit-backdrop-filter: blur(25px) saturate(180%);
            border-radius: 25px; /* Rounded corners like your screenshot */
            border: 1px solid rgba(255, 255, 255, 0.4);
            padding: 15px 25px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            box-shadow: 0 15px 35px rgba(0,0,0,0.2);
            z-index: 10000;
        }

        .logo-box {
            display: flex;
            align-items: center;
            gap: 10px;
            text-decoration: none;
        }

        .logo-img {
            height: 35px; /* Match mobile scale */
            width: auto;
        }

        /* Bold black text for the glass effect */
        .brand-text {
            color: #000;
            font-weight: 900;
            font-size: 1.1rem;
            letter-spacing: -0.5px;
        }

        .nav-links {
            display: none; /* Hidden on mobile by default */
        }

        /* Mobile Menu Button */
        .menu-btn {
            background: #000;
            color: #fff;
            padding: 8px 18px;
            border-radius: 50px;
            font-weight: 700;
            font-size: 14px;
            text-transform: uppercase;
        }

        /* --- FOOTER (Scrollable with Body) --- */
        .normal-footer {
            margin-top: 100px;
            padding: 40px 20px;
            text-align: center;
            border-top: 1px solid rgba(255,255,255,0.1);
        }

        .social-grid {
            display: grid;
            grid-template-columns: repeat(4, 1fr);
            gap: 10px;
            max-width: 400px;
            margin: 20px auto;
        }

        .social-box {
            background: rgba(255,255,255,0.05);
            padding: 15px;
            border-radius: 12px;
            font-size: 20px;
            text-decoration: none;
        }
    </style>
</head>
<body>

    <header class="constant-glass-nav">
        <a href="#" class="logo-box">
            <img src="my_new_logo_AdsNord.png" alt="AdsNord Logo" class="logo-img">
            <span class="brand-text">adsnord.com</span>
        </a>
        <div class="menu-btn">Menu</div>
    </header>

    <main style="padding-top: 150px; text-align: center; padding-left: 20px; padding-right: 20px;">
        <h1 style="font-size: 3rem;">Ads Nord</h1>
        <p style="color: #94a3b8; font-size: 1.2rem;">Watch the header stay put while you scroll.</p>
        
        <div style="height: 1000px; margin-top: 50px; border-left: 2px dashed rgba(255,255,255,0.1);">
            <p>Scrollable content area...</p>
        </div>
    </main>

    <footer class="normal-footer">
        <div class="social-grid">
            <a href="#" class="social-box">📘</a>
            <a href="#" class="social-box">📸</a>
            <a href="#" class="social-box">💼</a>
            <a href="#" class="social-box">🐦</a>
            <a href="#" class="social-box">📺</a>
            <a href="#" class="social-box">🎵</a>
            <a href="#" class="social-box">📌</a>
            <a href="#" class="social-box">💬</a>
        </div>
        <p style="font-size: 12px; color: #666; font-weight: bold;">
            PRIVACY | TERMS | CONTACT <br>
            © 2026 ADSNORD | ALL RIGHTS RESERVED
        </p>
    </footer>

</body>
</html>
