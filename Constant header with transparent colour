<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        body {
            margin: 0;
            padding: 0;
            background: #020617; /* Deep midnight base */
            font-family: 'Inter', sans-serif;
            height: 300vh; /* For scroll testing */
            /* Intense Aurora Background */
            background-image: 
                radial-gradient(at 10% 20%, rgba(66, 133, 244, 0.4) 0px, transparent 50%),
                radial-gradient(at 90% 10%, rgba(52, 168, 83, 0.4) 0px, transparent 50%),
                radial-gradient(at 50% 50%, rgba(251, 188, 5, 0.2) 0px, transparent 50%);
            background-attachment: fixed;
        }

        /* --- THE ULTRA-TRANSPARENT CONSTANT HEADER --- */
        header.aurora-glass-nav {
            position: fixed;
            top: 20px;
            left: 50%;
            transform: translateX(-50%);
            width: 92%;
            max-width: 1200px;
            
            /* High Transparency + Strong Blur */
            background: rgba(255, 255, 255, 0.3); 
            backdrop-filter: blur(30px) saturate(200%);
            -webkit-backdrop-filter: blur(30px) saturate(200%);
            
            border-radius: 30px;
            border: 1px solid rgba(255, 255, 255, 0.3);
            padding: 12px 20px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            z-index: 10000;
            box-shadow: 0 8px 32px 0 rgba(0, 0, 0, 0.3);
        }

        .logo-only {
            display: flex;
            align-items: center;
        }

        .logo-img {
            height: 42px; /* Prominent icon size */
            width: auto;
        }

        /* Bold Black Menu Button from your reference */
        .black-menu-btn {
            background: #000000;
            color: #ffffff;
            padding: 10px 24px;
            border-radius: 50px;
            font-weight: 800;
            font-size: 15px;
            letter-spacing: 0.5px;
            text-transform: uppercase;
            border: none;
            cursor: pointer;
        }

        /* Hero Content */
        .hero {
            padding-top: 180px;
            text-align: center;
            color: white;
        }
    </style>
</head>
<body>

    <header class="aurora-glass-nav">
        <div class="logo-only">
            <img src="my_new_logo_AdsNord.png" alt="AdsNord Icon" class="logo-img">
        </div>
        <button class="black-menu-btn">Menu</button>
    </header>

    <main class="hero">
        <h1 style="font-size: 3.5rem; margin: 0;">Ads Nord</h1>
        <p style="opacity: 0.7; font-size: 1.2rem;">Scroll down to see the aurora colors bleed through the glass.</p>
    </main>

</body>
</html>
